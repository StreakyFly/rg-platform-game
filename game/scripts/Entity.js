// import { vec3 } from 'lib/gl-matrix-module'

export class Entity {
    constructor(interactive, position = [0, 0, 0]) {
        this.interactive = interactive;  // bool
        this.position = position;  // x: float, y: float, z: float
    }

    move() {

    }
}